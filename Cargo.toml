[package]
name = "foolbox"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.release]
#lto = true
strip = true
codegen-units = 1

[dependencies]
fixed_wide = { version = "0.1.0", path = "../../strafesnet/fixed_wide_vectors/fixed_wide" }
